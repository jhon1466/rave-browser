<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base. La UI real se pinta con Compose (RaveTheme); esto solo evita
         la barra de acción y fija el fondo durante el arranque. -->
    <style name="Theme.Rave" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
